import React, { useState } from "react";
import {
  StyleSheet,
  Text,
  View,
  Button,
  TouchableOpacity,
  Alert,
  FlatList,
  TextInput,
} from "react-native";

export function Signout() {
  return (
    <TouchableOpacity>
      <Text>Sign out</Text>
    </TouchableOpacity>
  );
}
